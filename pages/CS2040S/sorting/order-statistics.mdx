# Order Statistics

Finding k-smallest/k-largest elements 

# Arbitrary Array

- Iterate through the array and keep track of top k elements
- Time: $O(N)$ time complexity as we iterate through array

# Sorted Array

- Simple `array.get(k)`
- Time: $O(1)$ time complexity, indexing