# Uniqueness Operation

Check if all elements in an array are unique

# Arbitrary Array

- Difficult to implement
- Requires auxiliary data structure to facilitate

# Sorted Array

- Implementable in $O(N)$ time complexity.
- Relatively easy to implement removal of duplications in $O(N)$ time complexity
    - Use separate array to maintain $O(N)$  speed
    - Using `remove(i)` method of ListArray will be costly → $O(N^2)$
